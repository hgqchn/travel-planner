# 高德地图接入与使用

本项目使用高德 JS API 2.0 展示地图，通过 Python 后端调用高德 Web 服务完成地点搜索和步行、驾车、公交/地铁规划。没有新增 npm 或 Python 依赖。

## 1. 在高德控制台创建凭据

1. 登录[高德开放平台](https://console.amap.com/)，完成控制台要求的开发者认证。
2. 在“应用管理 → 我的应用”创建应用，例如“旅行规划”。
3. 添加平台为 **Web 端（JS API）** 的 Key，记录 Key 和配套的 **安全密钥 securityJsCode**。
4. 在同一应用添加平台为 **Web 服务** 的另一个 Key，用于后端地点搜索和路线计算。
5. JS Key 如启用了域名白名单，按控制台格式添加实际访问的开发域名/主机和上线域名；Web 服务 Key 如启用 IP 白名单，填写后端服务器的**公网出口 IP**。不要把 `127.0.0.1` 或局域网 IP 当成公网出口。

参考：[创建 Web 服务 Key](https://lbs.amap.com/api/webservice/create-project-and-key)、[JS API 准备工作](https://lbs.amap.com/api/javascript-api-v2/prerequisites)、[JS 安全密钥代理](https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode)。

## 2. 本机配置

在项目目录的 `data/amap.env` 中填写：

```dotenv
AMAP_JS_KEY=填写Web端JS_API的Key
AMAP_SECURITY_JS_CODE=填写该JS_Key配套的安全密钥
AMAP_WEB_SERVICE_KEY=填写Web服务的Key
AMAP_DAILY_LIMIT=300
```

本次已经从你提供的 `高德地图.md` 写入以上配置，无需重复填写。原文件保留；它与 `data/amap.env` 都已排除出 Git 和 Docker 构建，文件权限为当前用户读写。

`dev/start-local.sh` 自动加载 `data/preview.env`、可选的 `data/deepseek.env` 与 `data/amap.env`。修改密钥后要重启 Python 服务。

```sh
cd /Users/houguoqing/Documents/demo/travel-planner
bash dev/start-local.sh
```

若 8000 端口已被原服务占用，先在原服务终端按 `Ctrl+C` 结束服务，再运行启动命令。打开 [本机页面](http://127.0.0.1:8000)。

Docker / 服务器部署：将相同配置项加入部署实际使用的环境文件（参考 `deploy/app.env.example`），重新构建、重建容器或重启服务。反向代理必须把 `/_AMapService/` 请求一并转发给本项目后端；项目现有整站代理模板支持这一点。

## 3. 使用地图与路线

1. 选择城市，在某天点击 **规划路线**。顶部为地图，下方为可左右滑动的当天行程，地点之间显示交通方式与规划后的耗时。
2. 点击行程或路段，下面只显示该段起终点。点击地点会选中下一段，末站选中上一段。分别搜索、按地址确认景点入口或餐厅分店；旧版多地点活动需先拆分。
3. 选择 **公交 / 地铁、步行、骑行、驾车**（默认公交 / 地铁），再点击 **规划本段路线**。无需确认当天所有地点。结果同时更新地图和行程条，切换路段保留本次已查询的其他结果。
4. 停留时长沿用 AI 建议，可人为修改并点击 **保存停留时长**。不再填写预约时刻或预约入场窗口。未知时长不自动假定为 60 分钟；无具体地点的休息只占时间，不生成虚构路段。
5. 前段变化导致后续公交参考出发时刻变化时，后段标记过期，等用户逐段重新规划。**查看全程地图**可查看所有已查询路线。
6. 展开 **全天预算与顺序优化**，按需核算完整交通、停留、开放窗口和缓冲。**优化顺序**生成待确认候选，只有点击 **应用此方案** 才更新共享顺序，**返回已保存顺序**放弃预览。单段查询不代表全天可行。

确认的 POI、停留和交通方式保存到共享日计划，关闭后重新打开仍可使用。地图读取 `/api/day-plan`，保存带日计划版本；核算和方案应用分别通过 `/api/day-plan/evaluate`、`/api/day-plan/apply` 完成。路线展示使用当前查询结果，重新打开需重新规划。

同行者修改当天行程或设置后，旧结果失效，当前输入保留并暂停保存和规划。点击 **载入最新日计划** 才替换为最新版；必要时先记下自己的未保存输入。切换城市或身份会关闭地图窗口。自己的保存和协作刷新不会误触发冲突。

公交按计划日期及每段预计出发时刻查询；驾车为查询时的耗时估算。该版本不自动核验开放时间、门票预约和列车余票，优化也不保证数学上的全局最优。

## 4. 控制成本

- 地图按需加载；地点搜索、路线计算由用户搜索、规划或切换方式触发，不随 5 秒协作刷新反复请求。
- 搜索每次最多返回 5 个候选；单段更新通常查询 1 段，必要时刷新后续已规划公交。全日核算包括首末接驳，顺序优化可能比较多个候选，消耗会随地点和候选数量增加；不要把优化当成一次路线请求。
- 后端请求间隔至少 0.4 秒；默认每天最多 300 次后端高德请求，失败请求也计数。这是本服务的保护预算，按北京时间零点重置，进程重启也会重置；多进程/多实例并不共享计数。
- JS 底图直连的请求不计入此预算，实际额度、收费和许可仍以高德账号控制台为准。可以按使用情况调小 `AMAP_DAILY_LIMIT`，不要把该配置当成高德账单的硬上限。

## 5. 排错

| 现象 | 检查方法 |
| --- | --- |
| 提示尚未配置 | 确认三项配置不为空，并已重启；启动脚本会自动读取本机文件 |
| `10001` | 检查 Key 是否有效、是否复制完整 |
| `10005` | 检查 Web 服务 Key 的公网出口 IP 白名单 |
| `10009` | 检查是否混用了 Web 端和 Web 服务的 Key |
| `10003` / `10004` | 查看控制台配额和 QPS，减少查询或等待恢复 |
| 地图不加载、搜索可用 | 检查 JS Key、安全密钥、域名白名单、网络和 `/_AMapService/` 的反向代理 |
| 无可用路线 | 确认入口与日期/时间，尝试其他交通方式；不要把失败结果理解为无需交通时间 |
| 本服务预算耗尽 | 查看使用量，按需要调整预算或次日再用 |

Web 服务 Key 和 JS 安全密钥只留在后端。公开的 JS Key 由配置接口提供给浏览器；安全代理严格限定官方主机和必要路径，不接受任意转发地址。地图查询接口需要项目口令与当前身份。

开发验证解释器：`/Library/Developer/CommandLineTools/usr/bin/python3`。适配器与接口测试位于 `tests/test_amap.py`；地图节点、共享保存、版本冲突和候选预览测试位于 `tests/test_trip_maps.cjs`、`tests/test_trip_maps_ui.cjs`；全天核算测试位于 `tests/test_daily_planning.py`。

每个路段的起点、终点均可点击“地图选择起点 / 终点”，在地图上点击入口或已有标记，确认后才保存定位。取消或切换路段不会保存未确认位置。定位属于该行程，在相邻路段中共享；更换后需重新规划受影响路段。任意地图选点以坐标保存，不自动推断地点名称或入口开放情况。

骑行使用高德 `/v4/direction/bicycling`，按返回的骑行耗时和折线展示；不可用时提示失败，可重试或更换交通方式。参考：[高德路径规划文档](https://lbs.amap.com/api/webservice/guide/api/direction)。
