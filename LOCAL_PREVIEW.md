# 本地预览

景点、美食已加入固定主分类、多个标签与标签筛选，美食另有菜系字段。编辑时可用逗号、顿号或换行输入标签，也可点击建议标签；筛选支持“符合任一标签”和“符合全部标签”，可与主分类组合，菜系也可作为标签筛选。AI 批量生成和单条补充已接入新字段，特色菜肴会尽可能填写具体菜系，补充结果仍需核对保存。

本次升级前完整备份位于 `data/preview/backups/place-taxonomy-20260910/`。现有预览项目已执行分类归并并保留原始迁移记录；139 条原有内容的 ID、数量及分类／标签／菜系以外的业务字段已与备份核对，已删除项目归档保留原样。回归测试通过，浏览器已验证标签去重保存、组合筛选、菜系显示和手机宽度排版；AI 验证使用模拟响应，没有调用真实模型。

景点现支持 4A/5A 景区级别：保存时自动匹配本地官方名录，已有景点读取时也会补充；AI 草稿支持级别选择。名录范围与截至日期见 [SCENIC_RATINGS.md](SCENIC_RATINGS.md)。景点仅保留高德导航，交通去除高德网页搜索，美食仅保留复制搜索词。

本机预览地址为 <http://127.0.0.1:8000>，管理员入口为 <http://127.0.0.1:8000/admin.html>。预览监听 `0.0.0.0`，局域网其他设备可通过 `http://这台Mac当前的局域网IP:8000` 访问。`TRIP_PUBLIC_ORIGIN` 留空，由后端校验请求的 Origin 与 Host 是否一致，无需固定局域网 IP；端口以 `TRIP_PORT` 配置为准。预览使用独立的 `data/preview/trip.db`；原本地数据库及阿里云线上数据不受影响。

## 启动与配置

当前 Mac 已生成 `data/preview.env` 和 `data/deepseek.env`，权限为仅本人可读写；这些文件均被 Git 和 Docker 构建忽略。登录口令见 `data/preview.env`，模型 Key 在 `data/deepseek.env`。

在项目目录启动：

```sh
TRIP_PYTHON=/Library/Developer/CommandLineTools/usr/bin/python3 ./dev/start-local.sh
```

启动终端按 `Ctrl+C` 可停止。修改 Python 后端或环境变量后需要重启，修改前端后刷新浏览器即可。若端口被已启动的预览占用，可直接访问页面，不必重复启动。

新机器先用 `python3 -c 'import sys; print(sys.executable)'` 确认 Python 3.9+，再设置 `TRIP_PYTHON` 为该解释器。没有额外 Python 或前端依赖需要安装。

首次配置示例（请填入自己的值）：

```sh
mkdir -p data
umask 077
cat > data/preview.env <<'EOF'
TRIP_PROJECT_CODE='你的本地项目口令'
TRIP_ADMIN_PASSWORD='你的本地管理员密码'
TRIP_PORT=8000
EOF
cat > data/deepseek.env <<'EOF'
DEEPSEEK_API_KEY='你的DeepSeek Key'
DEEPSEEK_MODEL='deepseek-v4-flash-vision-exp'
EOF
```

项目口令仅在首次建库初始化，之后通过管理员页面修改。管理员密码始终取自环境配置，修改后重启生效。

## 建议体验顺序

已规划城市卡片现提供“编辑城市”。修改开始日期可整体平移该城市的全部行程，弹窗预览会显示调整后的所有出行日期；保存后继续进行多城市时间提醒。移除城市需二次确认，只删除该城市行程，保留地点、美食及城市目录。更新后重启后端并刷新页面即可使用。

项目顶部新增“本项目的旅行城市”，按已保存的行程汇总城市、日期与数量，可点击切换。多城市在同一天有安排时，保存、AI 导入或重排后会弹窗提示可能的时间冲突；点击“知道了，保留安排”即可关闭，不要求修改，自动同步也不会反复提示同一组安排。只有开始时间，无法精确判断行程结束与转场耗时，因此同日转场也会提醒。更新后需重启本地后端并刷新页面。

行程页现已加入“导出行程”：可选择当前城市或当前项目全部城市，下载 Word（`.docx`）或 Excel（`.xlsx`）。AI 草稿需先确认导入。更新后请重启本地后端并刷新浏览器；下载文件保留当时的已保存行程，后续修改需重新下载。文件生成无需安装额外依赖。导出功能验证使用临时数据库；本轮 191 项后端、105 项前端测试通过，Word 和 Excel 已完成独立读取及中文排版检查。

1. 点击顶部城市选择，试试“杭州”“hz”“浙江”，或手动添加一个城市。
2. 进入交通页，在北京、上海、广州等已收录城市之间切换，点击线路图，通过加减、滚轮或双指缩放查看站名。图下可查看来源、作者、许可、文件修订时间和图源备注；高德与百度入口仍定位当前城市。
3. 打开 AI 旅行助手，选生成类别、日期、天数、人数和偏好。
4. 生成后编辑草稿、取消不想导入的条目，再点击导入；切换到对应城市的景点、美食和行程页查看。
5. 管理员页面检查城市和用户管理。
6. 在行程页点击“AI 重新规划全部行程”，或在某天标题旁点击“AI 重新规划这一天”。完整重排可更改日期与天数，单日只处理指定日期；编辑新方案后确认替换。确认前原行程仍保留，生成期间有人修改对应范围会提示重新生成。
7. 在交通页点击“检查并更新线路图”，等待状态完成后查看检查时间；本地尚未下载的已收录城市显示“下载高清线路图”。更新失败时原图保留，可稍后重试；放大窗口中的旧图会保留到关闭，再次打开显示更新后的图。未收录城市仍可使用上方城市地图。

内置线路图位于 `public/assets/metro`，本地在线更新写入 `data/preview/metro_maps`，同一预览部署中的各项目共用。更新需由本机连接 Wikimedia Commons，连接失败不影响已有图和旅行资料。文件修订日期是社区图片的更新记录，不代表实时运营情况。

AI 调用使用项目配置的真实 DeepSeek Key，会消耗该账户的 API 额度。结果导入前均为草稿。生成中关闭弹窗不会取消后台任务，同一浏览器可重新打开查看。

刷新页面后打开助手可恢复当前用户、当前设备的最近任务，目的地仍绑定原城市；需为当前页面城市生成时点击“为该城市新建计划”。恢复的是服务端原始结果，尚未导入的手工编辑不会跨刷新保存。

城市已按本次要求精简为 14 个。原杭州等被移除城市的景点、美食已存入资料缓存，完整原内容保留在升级前备份及数据库归档中。上海新增的美食仍可直接查看。

管理员页面现在可创建独立项目。直接在主页输入该项目独立口令即可进入，无需完整项目链接；带 `?project=` 参数的分享链接仍可使用。不同项目必须设置不同口令。首次输入口令、选择用户后，进入上海即可看到缓存景点和美食。批量删除仅改变所在项目，之后新建的项目仍能从缓存带入资料。

管理员“全部项目”列表中，每个项目右侧均可点击“删除项目”，输入完整项目名后确认。删除当前项目会转到其余项目；全部删除后仍可新建项目。原项目链接失效，资料归档保留，其他项目与全局景点、美食缓存不受影响。项目有 AI 内容正在排队或生成时，等待完成后再删除。

当前验收累计通过 135 项后端测试和 62 项前端行为测试；浏览器已检查 14 城列表、批量勾选、管理员项目删除确认，以及单日重规划的日期锁定、草稿与替换范围说明。实际删除、跨项目复制和行程替换回归均使用临时测试库；本轮重新规划验证使用模拟模型，没有消耗真实 AI 额度。现有预览项目和旅行资料均保留。

## 后续部署

此次先在本地验收。上线时需同步仓库中的新 Dockerfile 到服务器运维目录，并在服务器 `/etc/travel-planner/app.env` 配置 `DEEPSEEK_API_KEY` 和 `DEEPSEEK_MODEL`，备份后构建与重建容器；步骤见 [DOCKER_DEPLOY.md](DOCKER_DEPLOY.md)。

轨道交通图本轮验收：54 个城市条目文件的格式、尺寸、内容哈希校验通过；新增24项后端与14项前端测试覆盖更新、权限、失败保留与城市切换。全国图源和缺口见 [METRO_MAPS.md](METRO_MAPS.md)。本地原项目数据在更新前已保存至 `data/preview/backups/metro-maps-20260910-1331/`。

外链体验已精简：景点仅保留“高德导航”，交通页保留高德当前城市地图、百度地图及轨道交通图；美食仅保留“复制搜索词”和可见的“城市 美食名称”文字，可自行粘贴到美团等应用。局域网 HTTP 也可尝试复制，失败时可长按文字手动复制。原有攻略与参考资料链接仍可使用。手机 App 的实际唤起仍需在已安装 App 的真机上确认。外链更新前备份为 `data/preview/backups/external-links-20260910/`。
